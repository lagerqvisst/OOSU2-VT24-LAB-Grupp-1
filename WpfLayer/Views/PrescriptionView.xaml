<Window x:Class="WpfLayer.Views.PrescriptionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfLayer.Views"
        mc:Ignorable="d"
        Title="PrescriptionView" Height="600" Width="1000">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="Return"/>
            </MenuItem>
        </Menu> 
            <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Vertical">
                <Label Content="{Binding PatientName}"/>
                <Label Content="{Binding PatientId}"/>
            </StackPanel>

            <Label Grid.Column="0" Grid.Row="1" Content="Available drugs in database"></Label>
            <DataGrid Grid.Column="0" Grid.Row="2" ItemsSource="{Binding Drugs}" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                
            </DataGrid>

            <StackPanel Grid.Column="0" Grid.Row="3" Orientation="Horizontal">
                <Button Content="Add" Width="50"/>
                <Button Content="Remove" Width="50"/>
            </StackPanel>

            <Button Grid.Column="0" Grid.Row="4" Content="Save Prescription" Width="100" HorizontalAlignment="Left"/>

            <Label Grid.Column="1" Grid.Row="1" Content="Added drugs"/>
            <ListBox Grid.Column="1" Grid.Row="2"></ListBox>

             <Label Grid.Column="2" Grid.Row="1" Content="Prescription History"/>
             <DataGrid Grid.Column="2" Grid.Row="2"></DataGrid>

        </Grid>
        
        
    </DockPanel>
    
</Window>
