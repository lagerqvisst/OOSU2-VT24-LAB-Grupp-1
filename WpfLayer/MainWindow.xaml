<Window x:Class="WpfLayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfLayer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <DockPanel LastChildFill="True">
        <!--MENY START -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="Logga ut" x:Name="menuSignout"></MenuItem>

            </MenuItem>
        </Menu>
        <!--MENY SLUT -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
            </Grid.RowDefinitions>

            <StackPanel Grid.Column="0" Grid.Row="0" Margin="20,5,5,5">
                <Label Content="Username "></Label>
                <TextBox Text="{Binding Path=UserName,UpdateSourceTrigger=PropertyChanged}" ></TextBox>
            </StackPanel>

            <StackPanel Grid.Column="0" Grid.Row="1" Margin="20,5,5,5">
                <Label Content="Password "></Label>
                <TextBox Text="{Binding Path=Password,UpdateSourceTrigger=PropertyChanged}"></TextBox>
            </StackPanel>

            <Button Grid.Column="0" Grid.Row="2" Content="Sign in" Width="50" HorizontalAlignment="Left"  Margin="20,5,5,5" 
                    Command="{Binding SignInCommand}"></Button>

            <TextBox Grid.Column="0" Grid.Row="3" Text="Inlogg Doktor: Tom Crane , wowdoctorimgood123" Margin="20,5,5,5"></TextBox>
            <TextBox Grid.Column="0" Grid.Row="4" Text="Inlogg Receptionist: Rebecca Smith , receptionistpass123" Margin="20,5,5,5"></TextBox>

        </Grid>


    </DockPanel>
</Window>
